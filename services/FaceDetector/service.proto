syntax = "proto3";

package face_detector;

service FaceDetector {
  rpc DetectFaces (Request) returns (Reply) {}
}

message Request {
  bytes image = 1;
  string filename = 2;
}

message Reply {
	repeated int32 detected_faces = 1;
}